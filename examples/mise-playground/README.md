# Mise playground

This directory is deliberately tiny. Its `mise.toml` demonstrates three
independent concepts:

1. `[tools]` selects an exact Node version;
2. `[env]` adds a non-secret project variable;
3. `[tasks.*]` names commands that run only when requested.

Follow the exercise in [`../../docs/MISE.md`](../../docs/MISE.md).
